# Optional dependencies for the separate DeepSeek-OCR extraction environment.
# Install this only in the OCR-only Lightning account. It may be incompatible
# with the Qwen2.5-VL environment, so keep the two environments separate.

addict
datasets>=2.21.0
easydict
einops
pillow>=10.0.0
safetensors>=0.4.0
torch>=2.4.0
tqdm>=4.66.0
transformers==4.46.3
